# SRL_process_output
Docker build containing python script to download fastq files, process them in parallel (fastqc and md5sum) and upload results. 
Python script makes use of ica APIs generated by openAPI generator. 
